{
  "name": "agentstack-desktop",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build --bundles app",
    "desktop:build:dmg": "tauri build --bundles dmg",
    "desktop:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "desktop:prepare-sidecars": "bash ./scripts/fetch-cloudflared.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-stronghold": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5",
    "vite": "^5.4.8"
  }
}
