{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the clawstack desktop launcher",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:resources:default",
    "core:webview:default",
    "core:window:default"
  ]
}
